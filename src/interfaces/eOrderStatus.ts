/**
 * Enumerators:
 * Is predefined list of accepted values in a objet key
 */
export enum eOrderStatus {
  infoReceived = "order-info-received",
  onTheWay = "on-the-way",
  ready = "ready-for-pickup",
  delivered = "delivered",
}
